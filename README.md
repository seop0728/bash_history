```bash
sudo curl -fsSL https://raw.githubusercontent.com/Seop0728/bash_history/main/monitor.sh -o /usr/local/bin/monitor && sudo chmod +x /usr/local/bin/monitor
```

### 터미널1(메인)
```bash
monitor auto
```


### 터미널2(모니터링)
```bash
monitor view
```
